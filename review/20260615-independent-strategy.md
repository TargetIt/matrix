# 独立策略复审处理记录

- [x] SGEMM 人工复用百分比：改为来源支持的机制状态。
- [x] CUTLASS 非法参数组合：改为合法 policy 预设，并增加共享模型校验。
- [x] CUTLASS 算术强度口径错误：拆分 A/B 主循环与包含 C 的完整强度。
- [x] 来源专题和教程公式漂移：统一使用 `assets/gemm-policy.js`。
- [x] occupancy 架构不明确：增加 A6000 和 Ampere 通用估算选项。
- [x] 向量访存事务表述过度：改为“触及 128B segment”，补充模型边界。
- [x] 颜色语义不一致：增加并接入统一硬件语义色板。
- [x] 来源证据不可校验：增加 SHA-256 manifest 和自动检查。
- [x] 缺少需求与模型基线：增加 `doc/requirements.md`。
- [x] 缺少可重复回归：增加共享模型单测、静态检查和一键脚本。
- [x] 桌面与移动端基础回归：10 个页面在 `1440x900` 和 `390x844` 下无
  JavaScript 异常和页面级横向溢出。
- [ ] 浏览器性能量化：需要记录 FCP、帧率和连续操作内存结果。
- [ ] 学习效果验证：需要 3 至 5 名目标用户参与。
