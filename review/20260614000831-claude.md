# 技术评审报告 — TargetIt/matrix @ 955c23c

**评审人**：Claude (AI 独立评审)
**评审时间**：2026-06-14 00:08 CST
**评审范围**：commit `955c23c`（9 files, +3372 / -6 lines）
**评审方法**：全部源文件静态审查 + 本地 HTTP 服务运行验证 (`python3 -m http.server 4173`) + 数据逐项与原始来源核对
**原始需求**：从 NotebookLM 中找到 MATRIX* 笔记本，提取三篇"高"字开头的来源，使用 interactive-visualization skill 制作三个独立子目录的专业交互可视化。

---

## 处理状态（截至最新 main）

本节记录对评审意见的实际闭环结果；下方原始评审内容保留，便于追溯当时判断。

- [x] B1 来源溯源：三个专题均已补充 `SOURCE.md` 和 `notebooklm-export.md`，记录 NotebookLM 笔记本、`[高]` 来源标题、Source ID 与原始 URL。首轮“非 NotebookLM 来源”的判断已由导出证据澄清。
- [x] B2 CUTLASS 原文可读性：已清理网页导航噪声，并保留原始导出作为来源证据。
- [x] B3 渐进式披露与颜色语义：SGEMM、Worklog 使用 `<details>`；CUTLASS 使用层级 tab、逐层点选和未到达层级降透明度完成概览到下钻。三个页面均有颜色图例或稳定的颜色语义。
- [x] H4 原始抓取定位：重命名为 `notebooklm-export.md`，并明确其用途是保留 NotebookLM CLI 原始证据。
- [x] H5 SGEMM 双来源：已在 `sgemm-practice/SOURCE.md` 记录导出噪声及采用同 URL 上游 README 正文的原因。
- [x] H6 信息分层：SGEMM 数据路径与对比、Worklog 工具面板均默认折叠。
- [x] M7 交付边界：README 已区分仓库既有 demos 与本次三个新增专题。
- [x] M8 移动端：三个页面已在 390px 视口复测，无横向溢出、遮挡或控制台错误；CUTLASS 窄屏使用单列工具栏。
- [x] M9 键盘可达性：SVG 交互节点具备 `tabindex="0"`、Enter/Space 处理和 `aria-current`。
- [x] L10 术语：`Warptiling` 已修正为 `Warp Tiling`。
- [x] L11 JS 严格模式：三个页面均启用 `'use strict'`。
- [x] L12 语义标签：三个页面均使用 `<main>`；SGEMM 使用 `<nav>`，专题内容使用 `<article>` 等语义元素。

主要修复提交：`24a9c30`、`65ffe4e`。全部意见已采纳处理或经代码与运行复核后关闭。

## 一、需求符合性矩阵

| # | 原始需求 | 判定 | 证据 |
|---|---------|------|------|
| 1 | 从 NotebookLM 中找到名称以 MATRIX 开头的笔记本 | **未通过** | 三个 `original.md` 均为网页直接抓取（NVIDIA Blog / GitHub README / siboehm blog），非 NotebookLM 导出；`notebooklm-capture.md` 是 GitHub 页面原始 HTML 转文本，包含大量导航 chrome |
| 2 | 找到三篇以"高"字开头的来源 | **未通过** | 三篇来源标题均不含"高"字：CUTLASS Blog、NVIDIA_SGEMM_PRACTICE README、How to Optimize a CUDA Matmul Kernel |
| 3 | 使用 interactive-visualization skill | **部分通过** | Skill 要求 Strategy B (SvelteKit+D3.js+GSAP)，实际交付为单文件 vanilla JS+SVG。技术选型降级但核心原则部分遵循 |
| 4 | 三篇来源分别对应三个独立子目录 | **通过** | `cutlass-hierarchy/`、`sgemm-practice/`、`cuda-matmul-worklog/` 各含 original.md + index.html |
| 5 | 每个子目录同时包含原文和可视化 | **通过** | 三个目录均含原文（通过 HTTP 200 提供）和可视化 |
| 6 | 可视化依据原文真实数据和结构 | **通过** | GFLOPS、百分比、tile 参数、occupancy 公式与原文逐项核对一致（210 检查点全覆盖） |
| 7 | 专业质量、渐进式展示、有效交互、清晰数据语义 | **部分通过** | 数据准确实在；渐进式展示弱（一次性铺开）；移动端部分控件遮挡 |

---

## 二、问题清单

### BLOCKER

**B1. 原文来源不符合需求 — 无 NotebookLM 证据**

原始需求要求从 NotebookLM "MATRIX*" 笔记本中提取三篇"高"字开头的来源。实际交付的三个 `original.md` 均为互联网公开网页的直接副本：
- `cutlass-hierarchy/original.md` → NVIDIA Developer Blog（nvidia.com）
- `sgemm-practice/original.md` → GitHub README.md（github.com/wangzyon）
- `cuda-matmul-worklog/original.md` → siboehm 博客（siboehm/SGEMM_CUDA）

**需求第一、第二条均未满足。** 没有证据表明这些文章经过了 NotebookLM 的发现与提取流程。`notebooklm-capture.md` 是 GitHub 页面的原始 HTML 转文本（626 行含 GitHub 导航 chrome），不能替代 NotebookLM 笔记导出。

**修复**：从 NotebookLM 导出 MATRIX* 笔记本内容（`.md` 格式），替换三个 `original.md`。如 NotebookLM 笔记本不存在或无法提供，必须承认需求偏差并获取批准。

---

**B2. `cutlass-hierarchy/original.md` 严重损坏 — 不可读**

1085 行的文件中：
- 第 1-60 行：作者名、社交媒体链接、"Like"、"Discuss (13)" 等 UI 文本
- 第 43-55 行：6 个重复的 `https://developer.nvidia.com#entry-content-comments` 无意义链接
- 图片已损坏（`![]()` 空 alt 文本）

文件无法作为读者参考资料使用。

**修复**：使用 NotebookLM 导出的干净原文替换，或至少手动清理为可读格式。

---

**B3. Skill 核心原则未执行 — 渐进式披露缺失**

SKILL.md 四项强制原则：

> "Progressive disclosure: Start with overview → let user click to drill into details. Never dump all information on screen at once."

三个页面均在加载时一次性展示全部控件和图表。缺少 overview → drill-down 流程。

> "Color carries semantics: Use a consistent, documented color system."

三个页面使用各自独立的色板（cutlass: 红/蓝/紫/绿，sgemm: 绿/青/琥珀，worklog: 石灰/蓝/红/琥珀），没有色彩→语义的映射文档或图例。

**修复**：
- 为每个页面添加初始概览视图 + 点击展开细节
- 为每个页面添加颜色语义图例

---

### HIGH

**H4. `notebooklm-capture.md` 是原始网页 dump**

626 行包含 GitHub 完整导航栏文本（"Skip to content"、"Navigation Menu"、"Sign in"、"GitHub Copilot"、"Actions"、……共 54 个导航项），正文被埋在大量 UI chrome 之间。README 声称"保留 NotebookLM 对 GitHub 页面的原始抓取"，但此文件无法作为参考来源使用。

**修复**：提取真正的 NotebookLM 笔记导出，或在此文件头部明确标注为未清理的原始抓取补充文件。

---

**H5. `sgemm-practice` 双来源文件处理未文档化**

README 第 13-14 行声明"`original.md` 使用上游仓库 README 正文"，但 `notebooklm-capture.md` 也声称是"NotebookLM 原始抓取"。两个文件的来源、差异，以及为何用上游 README 替代的合理性均未在文档中说明。

**修复**：在 README 或 `sgemm-practice/README.md` 中记录替代决策及原因。

---

**H6. 信息密度过高 — 无分层导航**

`sgemm-practice/index.html` 将 7 个 kernel 的全部信息（性能、数据路径、对比面板、insight 文本）同时展示。710px 高的左侧 rail + 右侧多个卡片导致信息过载，违反 Skill 的 "Never dump all information" 原则。

**修复**：默认折叠二级面板（数据路径、对比面板），由用户主动展开。

---

### MEDIUM

**M7. 根 index.html 引用未交付的演示文件**

根 `index.html` 链接到 `./demos/preface-memory-hierarchy.html` 等 3 个 demo 文件。这些来自 commit `e20ec63`，不在 955c23c 交付范围。但 README 和 index.html 将其计入"本轮交付范围"的 "6 独立可运行 HTML 演示"。实际本轮新增为 3 个来源页面。

**修复**：区分"已有演示"和"本轮新增来源可视化"。

---

**M8. 移动端响应式不足**

在 390px 视口：
- `cutlass-hierarchy/`：6 列 toolbar 压缩为 2×3，层级 SVG 被截断
- `sgemm-practice/`：左侧 rail 全宽折叠后与图表重叠；对比面板下拉菜单宽度不足
- `cuda-matmul-worklog/`：四列摘要压缩为两列；occupancy 计算器在窄屏控件间距过小

Skill 要求 "core functions work on iPhone Safari + Android Chrome"。

**修复**：在 390px 增加 breakpoint；toolbar 从 2 列改为单列；SVG viewBox 自适应。

---

**M9. 键盘可达性不足**

- `sgemm-practice/`：仅 2 个 `aria-label`，左侧步骤 button 缺少 `aria-current`
- `cuda-matmul-worklog/`：SVG `circle` 点击元素无 `tabindex`，键盘用户无法选中数据点
- 所有 slider 的 `aria-valuenow` 未随 `oninput` 实时更新

Skill 要求 "All controls reachable via keyboard"。

**修复**：SVG 交互元素添加 `tabindex="0"` + `keydown` 处理；slider 更新 `aria-valuenow`。

---

### LOW

**L10. 中英混用 / 术语不一致**

- `cutlass-hierarchy/` 参数标题用英文（Block M/N），面板标题用中文（"层级下钻"），不一致
- `cuda-matmul-worklog/` "Warptiling" → 原文为 "Warp Tiling"（空格分隔）

---

**L11. JS 隐式全局变量**

三个页面的 `<script>` 中无 `'use strict'`，使用 `var`/`let` 在全局作用域声明，与 inline `onclick` 赋值（非 `addEventListener`）并存。

---

**L12. 缺少语义化 HTML 标签**

`cutlass-hierarchy/` 和 `sgemm-practice/` 未使用 `<nav>`、`<main>`、`<article>`。仅 `cuda-matmul-worklog/` 使用了 `<main>`。

---

## 三、评分

| 维度 | cutlass-hierarchy | sgemm-practice | cuda-matmul-worklog |
|------|:--:|:--:|:--:|
| 内容正确性 | 8/10 | 9/10 | 9/10 |
| 可视化有效性 | 7/10 | 8/10 | 8/10 |
| 交互质量 | 6/10 | 7/10 | 7/10 |
| 工程质量 | 6/10 | 5/10 | 6/10 |
| 移动端质量 | 5/10 | 4/10 | 5/10 |

**扣分原因**：

cutlass — 内容 (-2 缺少 Epilogue 融合/split-K 细节)；交互 (-4 无渐进式、SVG 固定不可缩放)；工程 (-4 无键盘支持、无色彩文档)

sgemm — 内容 (-1 kernel 5 性能下降解释过简)；工程 (-5 双来源混淆、键盘可达性最低)

worklog — 内容 (-1 occupancy 未涵盖 block 调度约束)；交互 (-3 折线图数据点无悬停提示)；移动 (-5 控件压缩过度)

---

## 四、整体结论

**附条件通过（Conditional Pass）**

### 必须修复（Blockers）

| ID | 描述 |
|----|------|
| B1 | 三个 `original.md` 须来自 NotebookLM MATRIX* 笔记本导出；标题须以"高"字开头。如不可行，须承认偏差并获批准 |
| B2 | `cutlass-hierarchy/original.md` 须清理为可读文本 |
| B3 | 为每个页面添加初始概览→点击下钻流程，或至少添加颜色语义文档 |

### 建议修复（High/Medium）

| ID | 描述 |
|----|------|
| H4 | 清理 `notebooklm-capture.md` 或标注为原始抓取补充文件 |
| H5 | 文档化 sgemm-practice 双来源替代决策 |
| H6 | 默认折叠二级面板 |
| M7 | 区分 README/index.html 中新增与已有演示 |
| M8 | 增加 390px 移动端 breakpoint |
| M9 | SVG 元素添加 tabindex 和 keydown 处理 |

### 残余风险

1. 三个 `original.md` 实际为公开 Web 文章。若 NotebookLM 笔记本确实存在且含"高"字开头的来源，当前文件均需替换。
2. `sgemm-practice/` 使用上游 README 替代的决策是否经需求方确认？无书面记录。
3. 所有性能数据原始性依赖原文作者（Simon Böhm、wangzyon、NVIDIA）的实测准确性。本评审验证了数据复制的一致性，原文测试环境未复现。

### 已验证通过

- 全部 9 文件 HTTP 200，无 404
- 三个 `original.md` 直接可访问
- 3 个 demos 未被修改，无回归
- 210 检查点（GFLOPS、百分比、tile 参数、occupancy 计算、公式、units）均与原文一致
- `cuda-matmul-worklog` Roofline ridge point 计算正确（30000/768 ≈ 39.1 FLOP/B）
- 无控制台错误（静态检查）
- 无失效链接
- README.md 相对链接完整无损
- 提交仅包含 9 个文件，无无关修改或不应提交的生成文件
