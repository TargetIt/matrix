# 独立严格复审处理记录

**评审对象**：commit `4d0763e`
**原始结论**：Fail
**处理原则**：保留评审结论，逐项以代码、公式和运行证据关闭，不以其他评审的 Pass 覆盖本报告。

## Findings 闭环

- [x] High 1 - 390px 移动端溢出
  - 复核结论：未复现。根页及 Step 3/4/5 均满足 `scrollWidth == clientWidth == 390`，全部可见元素包围盒位于视口内。
  - 防御性增强：三个新演示增加 `min-width:0`、`max-width:100%`、长文本换行和横向溢出约束。

- [x] High 2 - Step 3 缺第二次同步和 K-tail
  - 修复：状态机扩展为五阶段，增加计算后 `__syncthreads()`。
  - 修复：增加 K tile 轮次控制，最后一轮显示有效 K 槽和补零槽。

- [x] High 3 - Step 5 算术强度口径错误
  - 修复：A/B 主循环强度只计算 A/B tile 流量，因此不随 BK 改变。
  - 修复：增加完整 K 参数，单独报告包含最终 C 读写的完整 tile 强度。

- [x] High 4 - Step 5 下钻不是实际线程映射
  - 修复：点击 warp 会保存具体 warp ID。
  - 修复：Thread 层显示选中 warp 内 32 个 lane，并按 4×8 网格映射到各自 `TM×TN` accumulator tile。

- [x] Medium 5 - Step 4 最大参数视觉截断
  - 修复：移除 128 项绘制上限；`offset=7,count=128` 完整绘制索引 0 至 136。

- [x] Medium 6 - Step 3 内部 block 坐标不合法
  - 修复：首个完整 tile 固定从网格对齐坐标 0 开始；边界 tile 从 `floor((N-1)/T)*T` 开始。

- [x] Medium 7 - SVG 焦点不可见
  - 修复：增加 `.tile:focus-visible rect` 高对比白色描边，保留 Enter/Space 操作。

- [x] Low 8 - 根页性能图为静态手填百分比
  - 修复：替换为 RTX 3090、矩阵 5120 的来源实测数据：K1 15.65657%、K2 29.18283%、K4 84.79179%、K6 91.09125%、K7 94.36832%。

## 回归证据

- Step 3：300 个矩阵/tile/位置/K-round/阶段状态通过。
- Step 4：5,808 个向量宽度/偏移/长度/基地址组合通过。
- Step 5：144 个 block/warp/BK/K/缓冲 policy 组合通过。
- 所有组合无 `NaN`、`Infinity` 或页面运行错误。
- 根页及三个新增演示在 390px 均无页面或元素边界溢出。

## 结论

**处理后 Pass**。原 Fail 报告中的七项有效问题已修复；移动端溢出项经更严格自动断言判定为不可复现。
